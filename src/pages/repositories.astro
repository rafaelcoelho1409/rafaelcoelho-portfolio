---
import Base from "../layouts/Base.astro";
import StackBadge from "../components/StackBadge.astro";
import { REPOS, GITHUB_USERNAME } from "../data/repositories";

const STATS_BG = "0a0a0a";
const STATS_TITLE = "d4b572";
const STATS_TEXT = "cccccc";
const STATS_ICON = "d4b572";
const STATS_BORDER = "4a3a1f";

const statsUrl = `https://github-readme-stats.vercel.app/api?username=${GITHUB_USERNAME}&show_icons=true&hide_border=false&include_all_commits=true&count_private=true&bg_color=${STATS_BG}&title_color=${STATS_TITLE}&text_color=${STATS_TEXT}&icon_color=${STATS_ICON}&border_color=${STATS_BORDER}`;
const langsUrl = `https://github-readme-stats.vercel.app/api/top-langs/?username=${GITHUB_USERNAME}&layout=compact&hide_border=false&bg_color=${STATS_BG}&title_color=${STATS_TITLE}&text_color=${STATS_TEXT}&border_color=${STATS_BORDER}`;
const streakUrl = `https://github-readme-streak-stats.herokuapp.com/?user=${GITHUB_USERNAME}&background=${STATS_BG}&stroke=${STATS_BORDER}&ring=${STATS_TITLE}&fire=${STATS_TITLE}&currStreakLabel=${STATS_TITLE}&sideLabels=${STATS_TEXT}&dates=${STATS_TEXT}&currStreakNum=${STATS_TEXT}&sideNums=${STATS_TEXT}&hide_border=false`;
const trophyUrl = `https://github-profile-trophy.vercel.app/?username=${GITHUB_USERNAME}&theme=darkhub&no-frame=false&row=2&column=4&margin-w=8&margin-h=8`;

const grouped = REPOS.reduce<Record<number, typeof REPOS>>((acc, r) => {
  (acc[r.year] ||= []).push(r);
  return acc;
}, {});

const years = Object.keys(grouped)
  .map((y) => Number(y))
  .sort((a, b) => b - a);
---

<Base
  title="Repositories"
  description="GitHub repositories — every public project I've shipped, grouped by year, with key tooling per repo."
>
  <div class="container-page">
    <header class="mb-12 pt-4">
      <p class="eyebrow mb-4">/ Repositories</p>
      <h1
        class="font-display mb-4 text-[2.4rem] font-medium tracking-tight"
        style="color: var(--color-text); letter-spacing: -0.025em;"
      >
        Public repositories
      </h1>
      <p class="text-[16px] leading-[1.7]" style="color: var(--color-text-muted);">
        Every public GitHub project I've shipped, grouped by release year. Live stats
        cards below pull directly from GitHub.
      </p>
    </header>

    <section class="mb-14">
      <h2 class="label-mono mb-5">GitHub stats</h2>
      <div class="grid grid-cols-1 gap-3 sm:grid-cols-2">
        <a
          href={`https://github.com/${GITHUB_USERNAME}`}
          target="_blank"
          rel="noreferrer noopener"
          class="block overflow-hidden rounded-sm border transition-colors hover:border-[var(--color-gold)]"
          style="border-color: var(--color-border);"
        >
          <img
            src={statsUrl}
            alt={`${GITHUB_USERNAME} GitHub statistics`}
            class="block w-full"
            loading="lazy"
            decoding="async"
          />
        </a>
        <a
          href={`https://github.com/${GITHUB_USERNAME}?tab=repositories`}
          target="_blank"
          rel="noreferrer noopener"
          class="block overflow-hidden rounded-sm border transition-colors hover:border-[var(--color-gold)]"
          style="border-color: var(--color-border);"
        >
          <img
            src={langsUrl}
            alt={`${GITHUB_USERNAME} top languages`}
            class="block w-full"
            loading="lazy"
            decoding="async"
          />
        </a>
      </div>
    </section>

    <section class="mb-14">
      <h2 class="label-mono mb-5">Streak</h2>
      <a
        href={`https://github.com/${GITHUB_USERNAME}`}
        target="_blank"
        rel="noreferrer noopener"
        class="block overflow-hidden rounded-sm border transition-colors hover:border-[var(--color-gold)]"
        style="border-color: var(--color-border);"
      >
        <img
          src={streakUrl}
          alt={`${GITHUB_USERNAME} contribution streak`}
          class="block w-full"
          loading="lazy"
          decoding="async"
        />
      </a>
    </section>

    <section class="mb-16">
      <h2 class="label-mono mb-5">Trophies</h2>
      <a
        href={`https://github.com/${GITHUB_USERNAME}`}
        target="_blank"
        rel="noreferrer noopener"
        class="block overflow-hidden rounded-sm border transition-colors hover:border-[var(--color-gold)]"
        style="border-color: var(--color-border); background-color: var(--color-bg-2);"
      >
        <img
          src={trophyUrl}
          alt={`${GITHUB_USERNAME} GitHub trophies`}
          class="block w-full"
          loading="lazy"
          decoding="async"
        />
      </a>
    </section>

    <div class="hairline-gold mb-12" style="opacity: 0.35;"></div>

    <section>
      <h2 class="label-mono mb-7">Repositories by year</h2>
      <div class="space-y-12">
        {
          years.map((year) => (
            <div>
              <div class="mb-5 flex items-baseline gap-3">
                <span
                  class="font-display text-[28px] font-semibold tracking-tight"
                  style="color: var(--color-gold); letter-spacing: -0.02em;"
                >
                  {year}
                </span>
                <span
                  class="font-mono text-[11px] tracking-[0.14em] uppercase"
                  style="color: var(--color-platinum-dim);"
                >
                  {grouped[year].length} {grouped[year].length === 1 ? "repo" : "repos"}
                </span>
              </div>
              <div class="grid grid-cols-1 gap-3">
                {grouped[year].map((r) => (
                  <article
                    class="rounded-sm border p-5"
                    style="border-color: var(--color-border); background-color: var(--color-bg-2);"
                  >
                    <div class="mb-3 flex flex-wrap items-baseline justify-between gap-2">
                      <h3
                        class="font-display text-[17px] font-semibold tracking-tight"
                        style="color: var(--color-text); letter-spacing: -0.015em;"
                      >
                        {r.projectName}
                      </h3>
                      <span
                        class="font-mono text-[10.5px] tracking-[0.14em] whitespace-nowrap uppercase"
                        style="color: var(--color-platinum-dim);"
                      >
                        {r.releaseDate}
                      </span>
                    </div>

                    <div class="mb-4 flex flex-wrap gap-x-4 gap-y-1 font-mono text-[11.5px] tracking-tight">
                      <a
                        href={`https://github.com/${r.repo}`}
                        target="_blank"
                        rel="noreferrer noopener"
                        class="transition-colors hover:text-[var(--color-gold)]"
                        style="color: var(--color-text);"
                      >
                        github.com/{r.repo} ↗
                      </a>
                      {r.caseStudy && (
                        <a
                          href={r.caseStudy}
                          class="transition-colors hover:text-[var(--color-gold)]"
                          style="color: var(--color-gold-dim);"
                        >
                          Case study →
                        </a>
                      )}
                    </div>

                    <div class="flex flex-wrap gap-1.5">
                      {r.keyTools.map((t) => (
                        <StackBadge name={t} />
                      ))}
                    </div>
                  </article>
                ))}
              </div>
            </div>
          ))
        }
      </div>
    </section>
  </div>
</Base>
